name: torch_controller
description: A new flutter plugin project.
version: 0.1.3
homepage: https://github.com/4itworks/opensource_qwkin_dart

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  plugin:
    platforms:
#      android:
#        package: com.example.torch_controller
#        pluginClass: TorchControllerPlugin
      ios:
        pluginClass: TorchControllerPlugin